<h1>Create a new Pet</h1>

<form action="/pets" method="POST">
    <label>Name:</label>
    <input type="text" name="pet[name]" id="pet_name">

    <br><br>

    <label>Choose owner:</label>
        <% Owner.all.each do |owner| %>
            <input type="checkbox" name="pet[owner_id]" id="<%=owner.id%>" value="<%=owner.id%>"><%=owner.name%></input>
            <!-- owner.id is how we set the owner_id in params to owner_id
            could set @owners = Owner.all in controller, which way is better?
            pet is a hash, owner is a hash, and keys are db table columns 
            -->
        <% end %>

        <br><br>

        <label>and/or, create a new  owner:</label>
            <input type="text" name="owner[name]" id="owner_name">
            <input type="submit" value="Create Pet">
</form>